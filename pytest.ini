[pytest]
testpaths = tests
# tests/ is on sys.path so `from conftest import ...` works, and the repo root
# is too so `import extraction` resolves without an install step.
pythonpath = . tests
addopts = -q
